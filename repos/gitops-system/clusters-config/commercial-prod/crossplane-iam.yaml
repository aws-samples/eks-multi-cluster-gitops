apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: commercial-prod-crossplane-iam
  namespace: flux-system
spec:
  dependsOn:
  - name: commercial-prod-eks-cluster
  interval: 1m0s
  path: ./tools-config/crossplane-iam
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  healthChecks:
  - kind: Role
    apiVersion: iam.aws.crossplane.io/v1beta1
    name: crossplane-role-commercial-prod
  - kind: RolePolicyAttachment
    apiVersion: iam.aws.crossplane.io/v1beta1
    name: crossplane-rolepolicyattachment-commercial-prod
  validation: client
  timeout: 2m
  patches:
    - target:
        group: iam.aws.crossplane.io
        kind: Role
        version: v1beta1
        name: crossplane-role
      patch: |-
        - op: replace
          path: /metadata/name
          value: crossplane-role-commercial-prod
    - target:
        group: iam.aws.crossplane.io
        kind: RolePolicyAttachment
        version: v1beta1
        name: crossplane-rolepolicyattachment
      patch: |-
        - op: replace
          path: /metadata/name
          value: crossplane-rolepolicyattachment-commercial-prod
        - op: replace
          path: /spec/forProvider/roleNameRef/name
          value: crossplane-role-commercial-prod
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-info-commercial-prod
        optional: false